name: Build OnePlus Ace kernel
on:
  workflow_dispatch:
    inputs:
      VARIANT:
        type: choice
        description: "选择编译的版本"
        required: true
        default: "sukisu"
        options:
          - "sukisu"
          - "ksunext"
          - "docker"
      VFS:
        type: boolean
        description: "是否启用手动钩子"
        required: true
        default: true
      BBG:
        type: boolean
        description: "是否启用 Baseband-guard"
        required: true
        default: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 1024
          swap-size-mb: 4096
          remove-dotnet: true
          remove-haskell: true
          remove-docker-images: true
          remove-android: true
          remove-codeql: true

      - name: Configure Git
        run: |
          git config --global user.name "Bruce Zhang"
          git config --global user.email "zttt183525594@gmail.com"
  
      - name: Set up Python 2.7
        uses: LizardByte/actions/actions/setup_python@master
        with:
          python-version: '2.7'

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt-get install binutils make python3 libssl-dev build-essential bc bison flex unzip libssl-dev ca-certificates xz-utils mkbootimg cpio device-tree-compiler git git-lfs wget gzip lld

      - name: 拉取 vendor
        uses: actions/checkout@v4
        with:
          repository: OnePlusOSS/android_vendor_mediatek_kernel_modules_mt6895
          ref: oneplus/mt6895_v_14.0.0_ace
          path: vendor_platform

      - name: 拉取 kernel
        uses: actions/checkout@v4
        with:
          repository: OnePlusOSS/android_kernel_5.10_oneplus_mt6895
          ref: oneplus/mt6895_v_14.0.0_ace
          path: vendor_platform/kernel-5.10

      - name: 拉取仓库
        uses: actions/checkout@v4
        with:
          path: repo

      # 添加 SukiSU Ultra
      - name: Add KernelSU-SukiSU Ultra
        if: ${{ github.event.inputs.VARIANT != 'docker' }}
        id: ksu_version
        run: |
          cd vendor_platform/kernel-5.10
          if [ "${{ github.event.inputs.VARIANT }}" = "ksunext" ]; then
            curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -
            cd ./KernelSU-Next
          else
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-main
            cd ./KernelSU
          fi
          KSU_VERSION=$(expr $(/usr/bin/git rev-list --count main) "+" 10606)
          echo "KSU_VERSION=$KSU_VERSION" >> $GITHUB_ENV
          echo "KSU_VERSION=$KSU_VERSION" >> $GITHUB_OUTPUT
          export KSU_VERSION=$KSU_VERSION
          sed -i "s/DKSU_VERSION=12800/DKSU_VERSION=${KSU_VERSION}/" kernel/Makefile

      - name: Force remove -dirty suffix
        run: |
          cd vendor_platform/kernel-5.10
          sed -i 's/ -dirty//g' scripts/setlocalversion
          sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' scripts/setlocalversion
          git add -A
          git commit -m "Force remove -dirty suffix from kernel version"

      - name: Apply SUSFS Patch
        if: ${{ github.event.inputs.VARIANT != 'docker' }}
        run: |
          cd vendor_platform
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android12-5.10
          cd kernel-5.10
          echo "正在给内核打susfs补丁"
          cp ../susfs4ksu/kernel_patches/fs/* ./fs/
          cp ../susfs4ksu/kernel_patches/include/linux/* ./include/linux/
          patch -p1 < ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android12-5.10.patch
          echo "完成"
      
      - name: Apply Baseband-Guard Patch
        if: ${{ github.event.inputs.BBG == 'true' }}
        run: |
          cd vendor_platform/kernel-5.10
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash

      - name: Apply Hide Stuff Patches
        if: ${{ github.event.inputs.VARIANT != 'docker' }}
        run: |
          cd vendor_platform
          git clone https://github.com/ShirkNeko/SukiSU_patch.git
          cd kernel-5.10
          cp ../SukiSU_patch/69_hide_stuff.patch ./
          echo "正在打隐藏应用补丁"
          patch -p1 -F 3 < 69_hide_stuff.patch

      - name: Apply VFS
        if: ${{ github.event.inputs.VFS == 'true' && github.event.inputs.VARIANT != 'docker' }}
        run: |
          cd vendor_platform/kernel-5.10
          cp ../SukiSU_patch/hooks/syscall_hooks.patch ./
          echo "正在打vfs补丁"
          patch -p1 -F 3 < syscall_hooks.patch
          echo "vfs_patch完成"

      # 配置信息
      - name: Add Configuration Settings
        run: |
          cd vendor_platform/kernel-5.10
          CONFIG_FILE=./arch/arm64/configs/mgk_64_k510_defconfig
          KERNEL_VERSION=5.10
          if [ "${{ github.event.inputs.VARIANT }}" != "docker" ]; then
            # SukiSU Ultra config
            echo "CONFIG_KSU=y" >> "$CONFIG_FILE"
            if [ "${{ github.event.inputs.VFS }}" = "false" ]; then
              echo "CONFIG_KPROBES=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_SUS_SU=y" >> "$CONFIG_FILE"
            fi
            # VFS config
            if [ "${{ github.event.inputs.VFS }}" = "true" ]; then
              echo "CONFIG_KSU_SUSFS_SUS_SU=n" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_MANUAL_HOOK=y" >> "$CONFIG_FILE"
            fi
            # SUSFS config
            if [ "${{ github.event.inputs.VFS }}" = "true" ]; then
              echo "CONFIG_KSU_SUSFS=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> "$CONFIG_FILE"
              echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> "$CONFIG_FILE"
            fi
            # BBG config
            if [ "${{ github.event.inputs.BBG }}" = "true" ]; then
              echo "CONFIG_BBG=y" >> "$CONFIG_FILE"
            fi
          fi
          # Docker support config (always enabled)
          echo "# Docker / Container runtime support" >> "$CONFIG_FILE"
          echo "CONFIG_CGROUP_DEVICE=y" >> "$CONFIG_FILE"
          echo "CONFIG_CGROUP_PIDS=y" >> "$CONFIG_FILE"
          echo "CONFIG_CGROUP_NET_CLASSID=y" >> "$CONFIG_FILE"
          echo "CONFIG_CGROUP_BPF=y" >> "$CONFIG_FILE"
          echo "CONFIG_BRIDGE=y" >> "$CONFIG_FILE"
          echo "CONFIG_VETH=y" >> "$CONFIG_FILE"
          echo "CONFIG_IP_NF_NAT=y" >> "$CONFIG_FILE"
          echo "CONFIG_NETFILTER_XT_MATCH_IPVS=y" >> "$CONFIG_FILE"
          echo "CONFIG_IP_VS=y" >> "$CONFIG_FILE"
          echo "CONFIG_IP_VS_PROTO_TCP=y" >> "$CONFIG_FILE"
          echo "CONFIG_IP_VS_PROTO_UDP=y" >> "$CONFIG_FILE"
          echo "CONFIG_NET_IPIP=y" >> "$CONFIG_FILE"
          echo "CONFIG_IP_NF_TARGET_MASQUERADE=y" >> "$CONFIG_FILE"
          echo "CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y" >> "$CONFIG_FILE"
          echo "CONFIG_OVERLAY_FS=y" >> "$CONFIG_FILE"
          echo "CONFIG_BLK_DEV_DM=y" >> "$CONFIG_FILE"
          # Remove check_defconfig
          sed -i 's/check_defconfig//' ./build.config.gki

      - name: Get Clang and build tools
        run: |
          git clone https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 --revision b669748458572622ed716407611633c5415da25c --depth=1 vendor_platform/kernel/prebuilts-master/clang/host/linux-x86
          git clone https://github.com/LineageOS/android_prebuilts_build-tools.git --depth=1 vendor_platform/kernel/prebuilts/build-tools
      
      - name: Fetch needed files
        run: |
          # Fetch dct tool
          cd vendor_platform
          mkdir -p vendor/mediatek/proprietary/tools/dct
          cd vendor/mediatek/proprietary/tools/dct
          git clone https://github.com/Mashopy/android_tools_dct.git python

      - name: Build
        run: |
          export MAKE_GOALS=Image
          export KCFLAGS="-Wno-error=unused-variable -Wno-error=unused-label -Wno-error=unused-function"
          set -o pipefail
          cd vendor_platform
          mkdir -p ./kernel/build
          cp ../repo/custom_build_kernel.sh ./kernel/build/custom_build_kernel.sh
          cp ../repo/build_kernel.sh ./kernel/build/build_kernel.sh
          chmod +x ./kernel/build/custom_build_kernel.sh
          chmod +x ./kernel/build/build_kernel.sh
          LTO=thin ./kernel/build/custom_build_kernel.sh
          echo "Build exit code: $?"
          KOBJ="out_krn/target/product/mgk_64_k510/obj/KERNEL_OBJ/kernel-5.10"
          echo "=== Kernel images ==="
          ls -la "$KOBJ"/arch/arm64/boot/Image* 2>/dev/null || echo "No Image found, listing:"
          find out_krn/ -name "Image*" 2>/dev/null | head -10

      - name: Make AnyKernel3
        run: |
          git clone https://github.com/Numbersf/AnyKernel3 --depth=1
          rm -rf ./AnyKernel3/.git
          set -e
          KOBJ="vendor_platform/out_krn/target/product/mgk_64_k510/obj/KERNEL_OBJ/kernel-5.10"
          if [ -f "$KOBJ/arch/arm64/boot/Image.gz" ]; then
            cp -f "$KOBJ/arch/arm64/boot/Image.gz" ./AnyKernel3/
            echo "Copied Image.gz from $KOBJ"
          elif [ -f "$KOBJ/arch/arm64/boot/Image" ]; then
            cp -f "$KOBJ/arch/arm64/boot/Image" ./AnyKernel3/
            gzip -f ./AnyKernel3/Image
            echo "Copied & compressed Image from $KOBJ"
          else
            echo "FATAL: No kernel image at $KOBJ"
            find vendor_platform/out_krn -name "Image*" 2>/dev/null | head -5
            exit 1
          fi
          ls -la ./AnyKernel3/Image* ./AnyKernel3/Image.gz 2>/dev/null

      # 下载 SUSFS 模块（Release）
      - name: Download Latest SUSFS Module from Release
        run: |
          wget https://github.com/sidex15/ksu_module_susfs/releases/latest/download/ksu_module_susfs_1.5.2+.zip
          mv ksu_module_susfs_1.5.2+.zip ksu_module_susfs_1.5.2+_Release.zip
          cp ksu_module_susfs_1.5.2+_Release.zip ./AnyKernel3/

      - name: Set output name
        id: file_name
        run: |
          echo "value=AnyKernel3_PKGM10_${{ github.event.inputs.VARIANT }}_${{ steps.ksu_version.outputs.KSU_VERSION }}${{ github.event.inputs.VFS == 'true' && '_VFS' || '' }}" >> $GITHUB_OUTPUT

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.file_name.outputs.value }}
          path: ./AnyKernel3/*
